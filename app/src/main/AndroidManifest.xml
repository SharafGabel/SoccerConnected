<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathieu.myapplication2" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Soccer Connected !"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name="activity.listEventActivity" >
        </activity>

        <activity android:name="activity.detailActivity" >

        </activity>

        <activity android:name="activity.MainActivity" >

        </activity>

        <activity android:name="activity.MenuApp" >

        </activity>

        <activity android:name="activity.searchPlayer" >

        </activity>

        <activity android:name="activity.LoginAuth">
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
           </activity>





    </application>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
</manifest>
